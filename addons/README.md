# Published add-ons

Place reviewed extensions in `addons/<stable_id>/`. Each directory must include
`blender_manifest.toml` and `__init__.py`. Do not put unfinished source here:
every extension in this directory is published after a successful main build.

No placeholder add-on is installed or published by this starter repository.
